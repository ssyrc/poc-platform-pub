# poc-platform v6.0.5

## UI fixes
- GPU node hostname input width is reduced so the GPU free/total probe stays inside the GPU node setting card.
- Added node cards now show nvidia-smi based free/total GPU count per host.
- The free/total GPU count on added node cards is read-only and updates automatically from host GPU probing.
