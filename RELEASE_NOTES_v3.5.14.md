# v3.5.14

## UI refresh

- Dashboard: moved recent runs to the bottom, changed the default chart to line, split controls/field selection to the left and chart rendering to the right.
- Dashboard pinned data: added log path copy button.
- Training / Inference tabs: compact neon-style 설정 & 실행 layout, right-side current configuration summary, visible node delete and hyperparameter buttons, redesigned dry-run switch.
- Inference tab: changed PD distributed inference selection from checkbox to a two-state mode switch.
- Hyperparameter modal: opens near the top of the current viewport to avoid scrolling down to find it.
- Removed nonessential explanatory subtitles/tooltips from testing setting headers.
