# poc-platform v6.0.7

## 변경 사항

- 테스트 탭의 `Log 확인` 표기를 `로그 확인`으로 변경했습니다.
- 로그 확인 제목 옆의 `run.log`, `log path 대기` 상태 배지를 제거했습니다.
- 로그 확인 구역 하단에 `로그 경로` 설명과 `copy / report / 대시보드 반영` 버튼을 일렬로 배치했습니다.
- 로그 확인 구역의 세로 높이가 실시간 모니터링 구역과 동일하게 맞춰지도록 조정했습니다.
- 테스트 실패 시 로그에서 핵심 에러 메시지를 추출해 결과 분석 구역에 표시합니다. CUDA OOM, NCCL error, RuntimeError, FATAL 등의 마지막 핵심 에러를 우선 표시합니다.
